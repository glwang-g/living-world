# ComputerScienceWorld 项目锚点

## 核心目标

这个项目不是普通的 Minecraft 克隆，也不是资源增长、居民数值和按钮事件组成的模拟面板。

目标是构建一个可以进入、观察、修改、运行和编程的计算世界：

> 一个有空间、状态、规则、时间、事件和代理的世界；玩家不仅游玩世界，也能逐渐理解并改写它。

## 不可偏离的四个原则

1. **世界必须可操作**：玩家的行动要改变空间或规则，而不是只让数字加减。
2. **规则必须可观察**：玩家应该能理解“为什么发生”，并能通过实验发现规则。
3. **结果必须有后果**：行动要留下可回看的世界事件，影响之后的状态、地点或代理。
4. **系统必须可编程**：长期方向是让玩家通过自动化、代理或代码构造行为，而不是永远手动点击。

## 当前路线

当前采用 Minecraft 早期作为“物理地基”，不是最终产品方向：

```text
方块空间 → 移动/挖掘/放置 → 昼夜与威胁 → 规则与信号 → 自动化 → 居民/代理 → 可编程世界
```

MUD 角色视角和历史视角仍然属于核心方向：

- 角色视角：玩家只能看到角色当前能感知的局部世界。
- 历史视角：查看完整事件流，包括角色当时不知道的事实。
- 未来代理视角：居民和程序读取同一个世界状态，执行自己的策略。

## 功能判断门槛

新增功能前必须回答：

1. 玩家是否做出了明确选择或构造？
2. 这个动作是否改变了空间、规则、事件或代理行为？
3. 玩家能否观察并理解这个变化？
4. 这个变化是否能被历史视角或事件流回看？
5. 未来是否能让一个代理读取或执行它？

如果只是“再加一个资源、按钮、等级或自动增长数字”，默认不要做。

## 当前 MVP 的正确位置

当前 2D 方块世界只是验证地基：

- 玩家可以移动
- 破坏和拾取方块
- 放置方块
- 昼夜变化
- 夜晚出现威胁
- 世界历史记录行动

下一步优先级不是装备、数值成长或更多怪物，而是：

1. 让方块、火把、水流、门等拥有可解释的规则。
2. 引入信号、机关和简单自动化。
3. 让居民/代理读取这些规则并行动。
4. 再开放脚本或行为编程，逐渐走向 Screeps。

## 明确的反偏离提醒

以下方向如果没有连接到世界规则或可编程性，属于偏航风险：

- 偷菜式定时收资源
- 纯数值升级和等级成长
- 只展示文案、不改变世界的事件
- 为了数量添加大量 NPC
- 独立于世界状态的战斗小游戏
- 只追求地图和 UI 的视觉热闹

可以做游戏性、冒险和打怪，但它们必须服务于“理解和改造计算世界”：

```text
战斗改变地形或代理关系
探索发现规则
建造形成机制
自动化减少重复劳动
历史记录解释系统如何演进
```

## 每次开发结束前的自检

用一句话描述本次改动。如果只能说“增加了几个按钮/数字”，就需要重新审视；理想描述应该类似：

> 玩家通过搭建导线改变了门的开关规则，居民因此改走另一条路线，历史视角记录了这次变化。

## 跨项目协作

Freexlib 三件套（swarm-space / living-world / xshow）共享一份世界契约：词汇表、
事件形态与分层映射统一放在同级 `freexlib-portal` 仓库的
`docs/world-contract.zh-CN.md`（英文版 `docs/world-contract.md`）。本仓库的
`world-core / world-runner / world-bot / world-protocol` 分层与契约保持一致；
新增规则、动作或事件概念时，先对照契约，变更后同步更新契约文档。
